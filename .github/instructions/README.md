# Instructions Packs

This baseline keeps stack-specific guidance out of the repo-wide instructions.

Add only the packs that match the repository:

- `python-backend.instructions.md`
- `infra.instructions.md`

If the repo uses other stacks, create matching path-scoped instruction files instead of overloading `copilot-instructions.md`.
